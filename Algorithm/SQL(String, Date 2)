https://school.programmers.co.kr/learn/courses/30/lessons/157342
자동차 평균 대여 기간 구하기

select
	CAR_ID
	, (
	ROUND(AVG(DATEDIFF(END_DATE, START_DATE) + 1), 1)
	) as AVERAGE_DURATION
FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY
GROUP BY CAR_ID
HAVING AVERAGE_DURATION >= 7
ORDER BY AVERAGE_DURATION DESC, CAR_ID DESC;

ROUND 함수 : round (숫자, 반올림할 자리 수)
round (숫자, n) : 소수 n번째 자리 수까지 표현, n+1번째 자리에서 반올림

round (1234.56, 1) : 소수 2번째 자리에서 반올림 => 1234.6
round (1234.56, -1) : 0번째 자리 수에서 반올림 => 1230

DATEDIFF (마지막 일, 시작일) // 시작일이 앞에 오면 음수가 된다
시작일 ~ 마지막 일 차이를 출력, 시작일은 포함하지 않는다.
