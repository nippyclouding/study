<커리큘럼>

JAVA
SQL
Git/Linux/Ai

프로그래머스 코딩테스트 연습 꾸준히 연습하기 (프로그래밍, sql 연습, 프로그래머스가 실제 환경과 가장 비슷하다)

프론트엔드 1 : html, css, js + jquery
백엔드 1 : servlet, jsp
백엔드 2 : 스프링 레거시
테스트 (단위, 성능 .. )
1차 프로젝트 : 쇼핑몰, 예약 프로그램 (조 단위, 레거시 기술들을 이용하여 프로젝트 진행)

프론트엔드 2 : react
백엔드 3 : 스프링부트 (스프링 security ..)
2차 프로젝트 : 금융 프로젝트 (리액트 + 스프링부트, 프론트 백엔드 분리, api 호출을 이용하여 진행)
최종 발표

+ 금융공학, 파이썬, 데브옵스 .. 
+ 취업 특강, 취업 지원 .. 

------------------------------------------------------------------------------------------------------------
<이클립스 설치, 설정 - Window>
JDK를 설치하면 설치 디렉토리의 bin 경로 속에 java.exe, javac.exe 파일이 있다.
환경 변수 설정 : 현재 경로가 어느 위치에 있던 특정 명령어(java.exe)를 실행시킬 수 있게 하기 위해 설정

시스템 환경 변수 편집 - 환경 변수
~ 사용자에 대한 사용자 변수 : 로그인한 계정(사용 중인 계정)에 대한 설정
시스템 변수 : 전체에 대한 설정

시스템 변수 - 새로 만들기 - java_home(이름), jdk17을 설치한 경로 입력
시스템 변수 - Path - 새로 만들기 - %java_home%\bin 입력, 확인
cmd - java -version 입력 후 버전 확인하기

컴파일 언어 : 소스 -> 컴파일 : 기계어로 변환되어 실행 // 실행 속도가 상대적으로 빠르지만 수정 시 재컴파일 필요(유지보수가 상대적으로 어렵다) 
인터프리터 언어 : 소스를 바로 실행 (읽고 번역, 실행을 위에서 아래로 한다.) // 실행 속도가 상대적으로 느리지만 수정 시 재컴파일의 필요가 없다. (js, python) 

자바는 컴파일 동작은 하지만 컴파일 언어는 아니다.
.java 컴파일 -> .class 파일 (바이트 코드)
왜 기계어로 번역되지 않는지 => JIT 컴파일러 (just in time)
소스 컴파일 -> 기계어가 아닌 바이너리 파일이 산출된다(.class 바이트 코드). -> 가상머신이 실행 

컴파일 시점 : .java 파일을 수정, 저장할 때 (ctrl s)
자바에서의 컴파일러 : javac
자바에서 실행 역할 : java

이클립스에서 실행(윈도우) : ctrl F11

프로젝트 경로 하위에 bin, src가 있는데 bin은 .class 컴파일된 바이너리 파일, src는 .java 파일이 존재한다.
자바에서 패키지는 디렉토리, 폴더와 동일한 의미이다.
java -cp 명령어에서의 cp : classPath
자바 설치 시 for Java Developers보다 for Web Developers로 설치하는 것이 좋다. (더 많은 기능이 있다.)
System.out.println() 단축키 : sysout + ctrl space (이클립스)
// 주석 단축키 : ctrl / (이클립스, 인텔리제이 동일)




