<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.project.member.MemberMapper">

	<select id="login" parameterType="kr.co.project.member.MemberVO" resultType="kr.co.project.member.MemberVO">
		SELECT * FROM member WHERE email=#{email} AND pwd=MD5(#{pwd})
	</select>
	
	<insert id="join" parameterType="kr.co.project.member.MemberVO">
		INSERT INTO member (
			email, pwd, name, gender, birthday,
			hp, zipcode, addr1, addr2, regdate, state	
		) VALUES (
			#{email}, MD5(#{pwd}), #{name}, #{gender}, #{birthday}, 
			#{hp}, #{zipcode}, #{addr1}, #{addr2}, NOW(), #{state}
		)
	</insert>
	
	<select id="emailCheck" parameterType="String" resultType="Integer">
		SELECT COUNT(*) FROM member WHERE email=#{email}
	</select>
	
</mapper>







